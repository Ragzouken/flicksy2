.sidebar-panel(data-tab-body="sidebar/project")
    .section
        h2 flicksy 
        p a tool for drawing and assembling graphical hypertext games
        h3 getting started
        ol
            li create some drawings in the <em>drawings</em> tab above
            li compose a scene in the <em>scenes</em> tab above
            li playtest and export your game in the <em>play</em> tab above
    .section
        h2 project details
        h3 name
        .button-row
            input(type="text" data-path="project/name")
        h3 export
        .button-row
            button(data-action="project/export/data") project data
            button(data-action="project/export/html") html file
        h3 publish
        .button-row
            button(data-action="project/publish/neocities/start") neocities
            button(data-action="project/publish/neocities/open" data-path="project/publish/neocities/open" disabled) open
    .section
        h2 switch projects
        h3 reset
        .button-row
            button.delete(data-action="project/new") blank project
            button.delete(data-action="project/demo") demo project
        h3 load project from file
        button(data-action="project/open") choose file
